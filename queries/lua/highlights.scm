;; extends

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "vim"))

;; Find References for LazyUtil
((identifier) @namespace.builtin
  (#eq? @namespace.builtin "LazyUtil"))

;; Find References for U
((identifier) @namespace.builtin
  (#eq? @namespace.builtin "U"))
